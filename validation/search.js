const Coordinates = require("../models/Coordinates");

module.exports = function getcoordinates() {
  let list = [];

  /*
  Coordinates.find({}).toArray((err, result) => {
    if (err) throw err;
    console.log(result);
  });
  */

  /*
 for await (const doc of Coordinates.find()) {
  list.concat(doc);
  }
  return list; */
};
